# AGENTS.md

## 必读
- 开始任何任务前，先读 `CONTEXT.md`（用户背景、学习进度、计划、配合方式）。
- 用户会从桌面 Codex 或微信（codex-weixin）发起会话，两边共用本文件作为记忆，上下文要保持一致。

## 规则
- 用中文回答，客观直接，不无脑顺着用户；用户明确要求指出错误和遗漏。
- 给任务前先给整体框架，再一步步来；涉及命令或代码，直接给可复制的完整内容。
- 每次学习任务结束后，把新进度同步更新到 `CONTEXT.md` 的"我已掌握的"和"接下来计划"两节，删除过时内容，保持简洁。
- 用户精力最好的时段是下午/晚上；任务别一次排太满，避免制造焦虑。

## 项目说明
- 这是学习 AI 应用开发的项目：`chat.py`（API 对话）、`rag/`（RAG 问答）、`agent_calc.py`（工具调用）、`rag_app.py`（Streamlit 网页版）。
- 当前目标：2026 年 9-10 月投 AI 应用开发实习；近期任务是部署 `rag_app.py` 到 Streamlit Cloud。
