# 学习进度日志

> 规则：每天学习结束更新一条；反馈必须客观（完成什么 / 做得好与不好 / 卡点 / 明天最小一步），不粉饰。

## 2026-09-19（账单排查 + 止血：公开应用被滥用）

- **起因**：DeepSeek 账单出现大量 `deepseek-v4-pro` 消耗（8/19 单日 pro 约 31 元，flash 仅约 4 元），怀疑 Codex 在用 pro
- **排查过程（全部有证据）**：
  1. **Codex 会话日志**：11 个会话、1225 次请求，**全部 `deepseek-v4-flash`，pro 请求 = 0**（唯一出现的 pro 字符串是"可选模型清单"文本，不是请求）
  2. **账单按 api_key 分列**：`CODEX` key（sk-9d22a***87eb）= **只有 flash**；`chatpy` key（sk-75255***fab4）= **pro 消耗主力**（8/19 每小时 pro 请求 22-31 次、输入缓存命中 1300 万-2800 万 token）
  3. **全盘搜索 chatpy key**：只出现在本仓库脚本（chat.py / local_key.py / rag2）+ `.streamlit/secrets.toml` → 后者是**公开部署的 Streamlit 应用**读 key 的地方
- **结论**：烧 pro 的通道 = **公开部署的 Streamlit 应用**（任何访问者提问都消耗 owner 余额）；**clawbot 走 CODEX key，与该问题无关，不受影响**
- **处置**：已在 Streamlit Cloud 删除应用（`ai-learning` / `master` / `rag/rag_app.py`）——云端网址失效、云端 secrets 一并删除；**仓库代码未动，可随时重新部署**；**未动任何 API key**（尤其没动 CODEX key，保护 clawbot）
- **待验证**：数小时后查看 `chatpy` key 是否还有新记录 → 无新记录即结案；若仍有则轮换该 key
- **规则沉淀**：公开部署的 AI 应用必须用**独立且限额的 key**，或加访问密码 / 访客自带 key；本地开发与公开部署的 key 必须分开

## 2026-09-15（从零写代码 → Self-Distill 跑通 + 四轮 prompt 实验）

- **完成**：
  1. **零基础写代码第一关通关**：`practice_01.py` 独立写出跑通（变量 + f-string + print）；`practice_02.py` 自己解决 3 个真实 bug（文件名混进 python、漏 `()`、命令行漏 `.py`）；"读文件 + 看报错"两块短板补齐；亲历乱码（PowerShell 默认 GBK 读 UTF-8 文件）
  2. **OfferAgent 第一步打通**：`me.txt`（1539 字符 / 51 行 / 3005 字节）+ `self_distill.py` 跑通，生成个人画像 `profile.md`
  3. **四轮改动实验**（每轮先预测后验证）：① 删输出板块 → 内容被**重新分配**而非消失 ② 模糊标签指令 → 17【事实】/6【推断】，把预测误标成事实 ③ 加定义和例子 → 24/19，误标全部修正 ④ 删掉输入"我缺什么" → 模型**没有编造**，改用"素材中未出现"表述 + 对应推断
  4. **证据留档**：`profile_v1 / exp1 / exp2 / exp3` 四份对照文件；面试答案库新增"评估集样本量 ±8%"应答
- **客观评价**：这是三周以来第一次完整闭环（学 → 跑 → 改 → 验证 → 留证据）。最有价值的不是代码跑通，而是用户自己验证出两条一手结论——「指令精度决定输出精度」「删输入不会让模型瞎编，但会改变表述方式」，这两条比任何教程都值钱
- **卡点**：投递仍为 0 家（9/15 是计划里的投递启动日）；算法停在 8 题
- **明天最小一步**：投出 1-3 家（Calix 优先，用现成的一页 PDF），投完再继续 OfferAgent 第二步（岗位匹配）

## 2026-09-14（26 天断层后的现状核对）

- **事实（已核实）**：
  1. 本仓库（ai-learning）及上级 Codex 目录均无 OfferAgent / me.txt / self_distill.py → 项目文件在别的工作区、别的设备，或尚未落盘
  2. 投递记录表停在 8/17 版本：所有 8/18 计划行仍是"未投" → 8/18 首投未见执行痕迹，此后无新增投递记录
  3. git 最后提交 8/17（2e37b1f），此后 26 天零提交；进度日志最后一条 8/19，此后 26 天零日志
- **用户自报进度**：算法 8 题（哈希 3 / 双指针 3 / 滑动窗口 2；滑动窗口最大值进行中）；OfferAgent（Self-Distill 求职 Agent）已建文件夹、写完 me.txt，准备写 self_distill.py
- **对照计划差距**：算法 8/40（计划 9/15 前 40 题）；Agent 项目 0 行代码（计划 9/5 上线）；投递 0 家（计划 8/18 首投、9/16 批量）
- **客观评价**：断层集中在"投递"——秋招里最贵的是时间窗口，不是代码质量。项目换向（行业雷达 → OfferAgent）可接受且故事更好讲（用自己当真实用户），代价是上线从 9/5 推到 9 月底；投递不能等它
- **修正**：① 投递优先、项目并行；② 项目锁定 OfferAgent，9/30 前可演示；③ 算法改"高频 30 题 + 每题能讲"，每周 ≥8 题，链表/二叉树/DP 前置；④ 每天 1 条日志 + 每周 ≥1 次 commit；⑤ 清旧账（BM25 eval 未验证 / 吴恩达 15 题未交 / 搭档协议未批）
- **待用户确认**：OfferAgent 项目路径；8/18 投递实际执行情况
- **补充（同日）**：用户带来 DeepSeek 的学习诊断（10 条疑问 + 边做边学 + 元模型库）。核对结论：方法层面 DeepSeek 与我们的守则/闭环已收敛，**不再新增方法论**；它漏掉的关键事实是——用户已有作品（rag2 + eval + 简历 PDF），缺的是投递动作（0 家），所以"缺作品"这个诊断重心是错的。
- **晚 · 零基础写代码通关（有真实证据）**：`practice_01.py` 独立写出并跑通（3 行：变量 + f-string + print，自己改了句子）；`practice_02.py` 连续解决 3 个真实问题——① 文件名混进 `python` ② `text=f.read` 漏括号（TypeError: object is not subscriptable）③ 命令行漏 `.py`。读文件 + 看报错两块短板补齐；标准答案：`len(text)` = 1539 字符 / 51 行 / 文件 3005 字节。守则第 4 条按用户授权改为「AI 写初版，你审 + 改 + 讲」；`offeragent/me.txt`（3005 字节）与 `self_distill.py` v1 初版已就绪，待用户做改动实验。
## 2026-08-18（抖音 11 条检索 + 计划修订 + 手机助手 v2）【补记】

- **完成**：
  1. 11 条抖音链接检索完成，结果存 `学习笔记/抖音链接读取结果-20260818.md`（5 条与 8/15 同源、6 条新情报、3 条图文正文待 App 内读）
  2. PLAN.md 追加 2026-08-18 计划修订（今日唯一一次，7 项采纳：30 秒亮点 / 灵魂问题 3 层证据 / 追问地图 6 连 / 单 Agent 优先 / 部署岗趋势 / 博客 2 篇 / 复盘铁律）
  3. AI 学习助手升级 v2：新增「弹药」tab（技术栈 / 五阶段路线 / 大厂真题 / AI 岗位地图 / W 学姐复盘 / 对照行动）+ 面试真题速记 + 概念速查补 12 词
- **客观评价**：检索验证了两件事——双非 Agent 应用层真实可行（W 学姐 2 周 6 offer）；面试考的是项目深度和"自己写的"证据。v2 在另一会话完成，当日未记日志，现补记
- **卡点**：洋洋洋啊×2、杨锦漪、17 岁创业、阴雨天 4 条图文正文需手机 App 内读后回传
- **剩余待办（顺延）**：Calix 等首投 3-4 家；BM25 eval 验证

## 2026-08-19（学习顺序迁移 + 偏印学习法）

- **完成**：
  1. 夏梦迪"数学物理高效学习顺序"视频正文抓不到，按标题+佐证检索到核心：理科四件事 = 看书 → 听课 → 做题 → 思考总结（另有刷题四步法、15 分钟考点思维导图），置信度中高
  2. 迁移到 Agent 学习并写入手机助手 v2：① 看书 = 搭框架（15 分钟导图）② 听课 = 概念 + 对照 rag2 文件 ③ 做题 = 写代码/跑 eval ④ 思考总结 = 三句话/复盘
  3. 偏印学习画像（十神通用版，非精确排盘）：天赋 = 悟性/逻辑/深度钻研/冷门差异化方向；弱点 = 多学少成/喜走捷径/三心二意；对策 = 学习守则五条已内置，无需改计划
- **客观评价**：偏印"多学少成"与历史行为（收藏不执行、三分钟热度、计划焦虑）高度吻合；守则 v1 恰好是对症系统，今天不占用计划修订权
- **卡点**：无出生数据，偏印分析为十神通用画像；要精确需提供生辰（bazi-ziwei 技能可排盘）
- **今日剩余一步**：用户终端跑 `python rag2\eval_rag2.py`；Calix 投递顺延项

## 2026-08-17（续 · 守卫验证 + 简历一页 PDF + 提交清理）

- **完成**：
  1. 验证工具调用守卫：`agent_tool_guard.py` 输出 5 行与预期一致（2 成功 / 3 失败）；`agent_calc.py` 修复验证通过——debug 显示模型输出 `{"tool":"subtract","args":{"a":345,"b":67}}`，最终回答"345 减 67 等于 278。"（extract_json 方案生效）
  2. 简历一页 PDF：微调 HTML 打印样式（padding 12/20px + 行距压缩），Edge 无头导出 `简历/余剑-应聘AI应用开发实习-本科.pdf`（1 页，221KB，pypdf 验证文字完整含 GitHub/上线链接）
  3. git 提交 597b0cb：5分钟投递操作卡 + 投递清单/记录表更新 + 简历 HTML/PDF；清理 `.tmp_douyin`（13 条抖音 HTML 转储约 15MB）与 edge_dump 临时文件（git 历史可恢复）
  4. 数据核对：当前向量库 **254 块**（桌面端加新笔记后重建），与简历/投递清单"254 块"一致；但 eval 基线（75/83/92%）是 251 块时测的，RAG 优化时须重跑确认
- **客观评价**：守卫从"写了"变成"验证过"，今天可放心用；一页 PDF 比两页更适合投实习。风险提示：① GitHub push 仍失败（github.com:443 连不上），本地领先 1 个提交，待网络恢复重试；② 文档在持续变化，投递前所有数字以"当前 254 块 + 重跑 eval"为准，别让简历和口头说法对不上
- **卡点**：push 待重试；投递动作本身需用户在牛客/BOSS/实习僧/邮箱完成（账号在自己手里）
- **明天最小一步**：8/18 首投 3-4 家（Calix 牛客 + 萌想 + GoalfyAI 邮件 + 硅基可选），投完填 `投递记录表.md`；RAG D2（BM25 混合检索）在首投后开始

## 2026-08-17（决策日 · 外部方案评估 + 全网交叉验证 + 最终裁决）

- **完成**：
  1. 逐条评估另一 AI 的"简历急诊+投递重算+逐日计划"，产出 `学习笔记/外部方案评估.md`（采纳 7 项 / 拒绝 6 项 / 改造 3 项）
  2. 全网交叉验证 6 项关键事实（投递时机/冷冻期/岗位需求/双非可行性/算法权重/薪资），产出 `学习笔记/最终裁决与证据链.md`：
     - 27 届时间线：7-8 月提前批、9-10 月正常批、10 月大规模投就晚了；南京 8 月在招岗位实证存在（AI 医疗 NLP 至 8/20、AI PM 实习 200-500/天）
     - 冷冻期：大厂真实存在（字节/美团 6 个月、阿里/腾讯部分 12 个月），但"彻底拉黑大厂"错误 → 按岗位类型分波（字节/腾讯项目导向 9 月中可投，华为统一笔试卷延后）
     - Agent/应用层岗位需求真实爆发（重庆紧缺目录 2748 名、智能体市场 CAGR 107%、Skillver 白皮书"Agent 稳定性工程"薪酬 +34%）
     - 双非无实习靠项目补位可行；南京 AI 应用实习 150-300/天 为真实区间
  3. 简历 v2 已含技术债务小节（固定切分/未混合检索/评估规模小 + 下一版计划）；Agent 项目已改"计划 9 月扩展多工具协同"
- **客观评价**：决策日的价值是终结了"两份方案谁对"的摇摆——证据指向同一条路（项目+分波+可执行强度），两份方案没有根本分歧，分歧只在强度与心态。做得好的：评估没有照单全收，也没有全盘否定；不足：8/15-8/17 三天花在规划/评估上的时间偏多，从明天起只执行不复盘式重规划。
- **卡点**：当前会话 python 执行被拒、git 写被拒（index.lock 权限）→ 代码验证与提交需用户在终端跑。
- **明天最小一步**：8/18 投出第一波 3-4 家（Calix + 萌想科技南京 + 网眼 + 硅基 BOSS 确认）——这是门禁线，投出去才算今天结束。

## 2026-08-17（晚 · 执行材料打磨 + 工具调用守卫）

- **完成**：
  1. 简历 HTML 已与 MD 同步（求职意向改"完整实现并部署 RAG 应用"、补技术债务小节、Agent 项目改"计划 9 月扩展多工具协同"）——`简历/余剑-简历-AI应用开发实习.html` 即为打印/PDF 版本
  2. 新增 `学习笔记/8月18日投递执行清单.md`：每家公司的渠道 + JD 关键词 + 现成打招呼语（Calix/萌想/网眼/硅基确认规则）
  3. 新增 `agent_tool_guard.py`：工具调用守卫（TOOL_SCHEMAS 参数合同 + safe_call 校验与错误捕获，约 30 行核心）
  4. `agent_calc.py` 集成守卫：JSON 解析 try/except + safe_call + 错误回填给模型继续重试；并把 SYSTEM 提示词改为具名参数输出（修复 args 列表 vs 对象不匹配问题）
- **客观评价**：守卫解决的是真实问题（模型输出 5 种坏情况：未知工具/缺参/类型错/非对象/非法 JSON，任何一个以前都会让 Agent 崩）；改具名参数是必要修正，否则集成必挂。局限：守卫是"能跑"级别，未做嵌套参数、bool 误判 int、超时等，属下一版内容。
- **卡点**：本会话 python 拒绝访问、git 写拒绝 → 未运行验证、未提交。
- **用户在终端要做的（今晚收尾）**：
  1. `python agent_tool_guard.py` —— 应输出 5 行，前 2 行 ok=True、中间 3 行 ok=False
  2. `python agent_calc.py` —— 应正常回答"345 减 67 = 278"
  3. `git add -A && git commit -m "8/17: 工具调用守卫+简历同步+8/18投递清单+最终裁决" && git push`
  4. 打开 `简历/余剑-简历-AI应用开发实习.html` → 打印/存 PDF

## 2026-08-17（晚 · 守卫联调：真实 bug 定位）

- **用户实测**：`agent_tool_guard.py` 输出与预期完全一致（5 行：2 成功 3 失败）；`agent_calc.py` 返回"达到最大循环次数，任务未完成"
- **根因定位（用户 + AI 协作）**：正则 `\{[^}]*"tool"[^}]*\}` 无法处理嵌套对象——参数改为具名对象后，`[^}]*` 在第一个 `}`（内层 args 的结束）就停，截断出的字符串少一个右括号，`json.loads` 必失败 → 每轮都判"非法 JSON" → 5 轮耗尽。守卫本身没问题（这正是它该挡的第 5 类错误：解析层坏输入）
- **修复**：`agent_calc.py` 去掉正则，新增 `extract_json()`（第一个 `{` 到最后一个 `}` 切片，天然支持嵌套）；加 `[debug]` 打印模型原始输出；错误回填信息补充格式示例
- **待用户重跑**：`python agent_calc.py` → 应看到 debug 输出 + 最终答案"345 减 67 = 278"

## 2026-08-17（深夜 · 技能安装完成 + BM25 混合检索代码就绪）

- **完成**：
  1. 读取微信端聊天记录（session 日志 3789 行，提取今日 16 条用户消息）确认进度
  2. Codex skills 安装完成并验证：Superpowers 14 个 + find-skills + grill-me + handoff + humanizer-zh = 19 个技能，本会话已生效（`~\.agents\skills` 目录确认）
  3. BM25 混合检索代码写完（4 文件）：`rag2/bm25.py`（从零实现，无依赖）+ `store.get_all()` + `retriever._hybrid`（0.7 向量 + 0.3 BM25 双路融合）+ `eval_rag2.py` 改为"纯向量 vs 混合"对比实验
- **客观评价**：今天产出密度高（决策→简历→守卫代码→技能→检索改造五连），但 BM25 是"写完≠跑通"状态——还没验证，属最大未完成项；另外简历 PDF、git 提交、8/18 投递三件事仍未落地
- **卡点**：本会话 python/git 受限 → 验证和提交需用户终端执行
- **下一步（最小一步）**：用户终端跑 `python rag2\eval_rag2.py`，看混合检索对比结果（重点："什么是 Agent？"是否从 0 命中变命中）

## 2026-08-16（阶段 A · eval 检索基线）

- **完成**：`python rag2\eval_rag2.py` 跑出检索基线（12 条评估集，top-5 召回 + TF-IDF 字符重排）：
  - top-1：9/12 = **75%**
  - top-3：10/12 = **83%**
  - top-5：11/12 = **92%**
  - 完全未命中 1 条："什么是 Agent？"（top-5 全空）；弱命中 1 条："南京有哪些 AI 应用开发实习机会？"（重排后 best=5，刚够 top-5）
- **根因定位（有数据，不是猜）**：问"什么是 Agent？"时，`大模型入门.txt` 的 Agent 定义块在全库 251 块里向量排名仅 **第 23（相似度 0.578）**，top-5 全被学习笔记里的 Agent 调研块占据（抖音链接清单等 50+ 块 Agent 关键词饱和）；且定义块是"Embedding 尾巴 + Agent + API"的 300 字混合块，Agent 语义只占约 1/3，向量被稀释。反证：同一块问"什么是 API？"能排到第 3（命中 top-3）——问题不在块本身，在"查询词 × 语料分布 × 混合切分"
- **客观评价**：92% top-5 对 12 条小评估集是可接受基线，但暴露两个真实问题：① 语料关键词饱和时纯向量检索会漏掉"干净的定义块"；② 300 字贪心切分稀释单主题语义。这两个问题正好是面试能讲深的"检索优化"故事，不白跑。局限：eval 只测"来源+关键词命中"，还没量化"答案质量"（下一层）
- **卡点**：无新卡点；eval 脚本本身是基线工具，已具备重跑对比条件
- **明天最小一步**：优化检索——首选"向量 + 关键词混合召回"（字符 n-gram / BM25），或按标题把定义类问答单独成块；改完重跑 `eval_rag2.py` 与本次基线对比

## 2026-08-15（赛道定位 + 计划 v4 定稿）

- **完成**：
  1. 三个抖音视频在电脑上实际打开核对，修正了半序qwq视频的结论（Java/C++ 基座 + 大模型，4 层就业层次）
  2. JD 调研扩到 30+ 家（A 档南京 16 / B 档远程 9 / C 档大厂 17），关键词三档整理完成
  3. PLAN v4 定稿：综合 JD 要求 + 学习路径 + 个人学习方式，含投递节奏与每日反馈机制
  4. rag2（RAG 重构 v2）全部代码写完：解析/切分/向量化/Chroma/召回/重排/FastAPI/网页版
- **客观评价**：今天信息密度高、方向彻底定死（Python AI 应用开发，南京优先），是好事；但 rag2 只写了代码、还没跑通验证，"写完≠能用"，这是目前最大的未完成项，明天第一件事就是验证
- **卡点**：GitHub 推送网络不稳（提交在本地，待重试）；eval 评估集还没写
- **明天最小一步**：8/16 跑通 rag2（入库 + 检索 + 问答）→ 通过后再写 eval 评估集

## 2026-08-15（晚 · 求职战术分析）

- **完成**：综合抖音收藏 100+ 链接（秋招/外企/Agent 强度/AI 变现），产出 `学习笔记/求职战术与AI变现分析.md`，PLAN 微调为 v4.1（外企路线、免笔试优先、Agent eval、场景题 10 道、AI 赚钱副线原则）
- **客观评价**：收藏里真正有用的信号是"Agent 岗强度共识"和"8 月底投实习不晚"；AI 赚钱类大多标题党，已明确现阶段不投入——这条边界很重要，避免被带节奏
- **卡点**：无新卡点；原有卡点不变（rag2 未验证、eval 未写）
- **明天最小一步**：不变——跑通 rag2，然后写 eval 评估集

## 2026-08-15（深夜第二稿 · 全量重读 + 未来推演）

- **完成**：重新逐条过完 126 条收藏；抖音链接确认无法抓取（诚实标注）；按标题+佐证研究完成四层推演（技术路径共识 / AI 趋势 / 未来判断 / 重评 idea）；建立 `我的Agent想法库.md` 把全部想法按趋势优先级存档；PLAN 增加深研引用
- **客观评价**：这次把"为什么选行业雷达、为什么不碰自我进化"的论据补硬了（垂直场景 Agent 是未来 2 年最先落地的形态；自我进化方向对但工程与岗位未到）；同时承认一个局限——抖音图文正文没读到，结论基于标题与外部佐证，存在误差可能，明天跑通 rag2 后如有精力可再补看
- **卡点**：同前（rag2 未验证、eval 未写；git 只读+断网）
- **明天最小一步**：跑通 rag2 → 写 eval 评估集

## 2026-08-16（阶段 A 第 1 天 · rag2 跑通）

- **完成**：`rag2` 全流程跑通——11 篇资料解析 → 251 块切分（300字/块、重叠50）→ bge 向量化（30.7s，二次重建 7.3s）→ Chroma 入库 → top-k 检索（向量分+重排分）→ DeepSeek 问答（带引用 [1]）；修复 1 个 bug（engine.py 缺 EMBED_MODEL import）；一键脚本 run_rag2.ps1 可用；离线模式已加（HF_HUB_OFFLINE）
- **客观评价（检索质量）**：跑通 ≠ 好用。测试暴露问题：
  - 问"什么是 RAG？"→ top-3 没有最相关的"RAG 定义块"（返回的是上下文窗口/Embedding/Token 块），LLM 答对靠的是模型自身知识+部分片段
  - 问"大模型是怎么工作的？"→ top-1 是"什么是 Token"块，不是"工作原理"块
  - 向量分集中在 0.5-0.6（区分度低）、重排分普遍 0.1-0.18（TF-IDF 字符重排对短问长块效果一般）
- **结论**：这正是 eval 评估集要度量的东西——先量化命中率，再优化（切分策略/top_k/重排/混合检索）
- **卡点**：当前微信会话受限（python/git 写均不可用），提交推送需全权限会话或电脑端
- **明天最小一步**：写 eval 评估集（10-20 条问题+标准答案段，测 top-3/top-5 命中率），跑出基线数字

## 2026-08-16（晚 · 新视频补充 + 执行版计划 v4.3）

- **完成**：收到 13 条新 Agent 主题视频（吴所知 3 问 / LucianAI 三连 / 鑫哥 6 个月 / 纳米香蕉 20 项目 / n8n / 电商 Agent / Agent PM / FDE 出海等），已提炼进深研笔记；PLAN 升 v4.3，阶段 A 按天拆解（D1-D5），新增"边做边开源""项目三问""Agent PM 备选轨道"
- **客观评价**：新视频没有推翻方向，反而加硬两点——① 项目要开源+可演示（为面试）② 匹配度 > 单场面试（广投复盘）；当前最大卡点不是学习内容，而是"跑 eval 的环境"：codex exec 两次超时，用户手动 PowerShell 反而能跑通 → 后续命令类任务优先走一键脚本/用户终端
- **卡点**：eval 基线还没出（codex exec 超时）；rag2 代码改动+新脚本未提交（受限会话 git 只读）
- **明天最小一步**：D2 优化检索第一轮（拿到基线后）——先让用户在终端跑 `run_rag2.ps1` 出基线

## 2026-08-16（晚 · 硬约束加入：8/9 月必须能投）

- **完成**：用户明确"8 月和 9 月要达到能投实习的地步"→ PLAN v4.4 加入"8 月投递门禁"（门禁判定 4 条 + 时间线：8/17 简历 v1 → 8/18 首投 2-3 家 → 8 月下旬每周 2-3 家 → 9 月秋招网申 + 批量）；修正旧矛盾（原"9/15 简历定稿"与"8/18 试投"冲突，现简历 v1 提前到 8/17）
- **客观评价**：这条约束是好事——它把"完美主义"关进笼子，也把计划从"理想版"逼成"可用版"；代价是今天任务变紧：eval 基线 + 自检 + 简历骨架三件事
- **卡点**：同前（eval 基线未出；受限会话跑不了 python）
- **明天最小一步**：8/17 完成简历 v1 + 首投准备；D2 检索优化照常

## 2026-08-15（深夜第三稿 · 浏览器权限受阻 + 网络实证补充）

- **完成**：确认当前会话无法控制桌面浏览器（受限模式，窗口枚举为 0）；改走网络检索，挖到 AgentGuide 2026 求职路线、企业级 Agent 评测实战、转型思维等实证，已补进深研笔记与 PLAN（eval 任务集结构、Web Research Agent 方向验证）
- **客观评价**：抖音正文仍没读到（受限），但网络实证与标题推断高度一致，计划结论不受影响；浏览器读取任务留给全权限会话（电脑端 Codex）或用户手动复制
- **卡点**：① 本会话浏览器控制不可用（等待全权限会话）② rag2 未验证、eval 未写 ③ git 只读+断网
- **明天最小一步**：跑通 rag2 → 写 eval 评估集

## 2026-08-15（深夜 · Agent 深度研究 + 选题定稿）

- **完成**：检索并细读腾讯 2027 校招拆解（Agent 开发独立岗位 JD 全文）+ 字节面经考点；把你的 50+ 条 Agent/skill 想法全量归类（A 求职/B 调研/C 创作/D 长线）；项目 2 定题 = 行业机会调研 Agent（行业雷达）；产出两份深度笔记并并入 PLAN v4.2
- **客观评价**：这次研究把"强度"从感觉变成了硬标准（JD 六条要求 + eval 体系 + 灵魂问题 + 博客）；你的想法里真正能当项目 2 的是"行业调研"，其余多为 skill 或长线——已有明确取舍，不再发散
- **卡点**：同前（rag2 未验证、eval 未写）；另：当前会话 git 只读+断网，新笔记只保存在本地，待下次全权限会话提交
- **明天最小一步**：不变——跑通 rag2，然后写 eval 评估集

## 2026-09-19（下午 · 权限打通 + 安装教学技能）

- **权限问题根治**：三项验证全部通过（① 写仓库以外文件 ② 终端联网 ③ 浏览器读网页）。根因查明：桌面版用"托管权限配置"（存在 `.codex-global-state.json` 的 `heartbeat-thread-permissions-by-id`，按对话一份），**不读** `config.toml` 的 `sandbox_mode`；且 `full-access` 选项默认在界面里隐藏。用户手动改为 `danger-full-access` + `approval_policy=never` 后生效。
- **新增教学类技能 6 个**（来自 GitHub）：
  1. `teach`（mattpocock/skills）— 多轮教学系统：MISSION.md / lessons / reference / learning-records / RESOURCES / NOTES
  2. `wait-what`（同上）— "上一条没听懂，换人话重讲"
  3. `grilling`（同上）— 拷问式追问（补全原先坏掉的 grill-me）
  4. `programming-tutor`（cdmorozov/claude-tutors）— 教编程：概念题直接讲，写代码/调 bug 时引导学员自己推理（"能跑的代码是陷阱"）
  5. `explain-my-own-code`（khaichonggg）— 七层讲解自己的代码（用途→位置→流程→概念→细节→边界→**让学员复述**）
  6. `doceo`（eugeniughelbur）— 把任何主题变成一屏可视化 HTML 课 + 配套笔记，带学习者记忆
- **踩坑记录**：`raw.githubusercontent.com` 和 `github.com:443` 在受限网络下不通，只有 `api.github.com` 可用 → 用 contents API + base64 下载文件；`git clone` 失败。
- **下一步**：用这 6 个技能重构学习方式（概念用 doceo、写代码用 programming-tutor、读懂自己项目用 explain-my-own-code、系统学用 teach）。

